# ──────────────────────────────────────────────────────────────
# SGFI — Variáveis de ambiente do frontend (Vite)
# Copie para .env.local e preencha. NUNCA faça commit do .env.local.
# ──────────────────────────────────────────────────────────────

# URL base da Firebase Function (deixe vazio em dev — o proxy do Vite cuida disso)
# Em produção (Vercel), preencha com a URL da Cloud Function:
# VITE_API_URL=https://api-HASH-southamerica-east1.a.run.app
VITE_API_URL=
