# ──────────────────────────────────────────────────────────────
# SGFI — Variáveis de ambiente
# Copie este arquivo para server/.env e preencha os valores.
# NUNCA faça commit do arquivo .env real.
# ──────────────────────────────────────────────────────────────

# ID do projeto Firebase (visível no Console Firebase > Configurações do projeto)
FIREBASE_PROJECT_ID=sgfi-sejuv

# ── Credenciais Firebase ───────────────────────────────────────

# OPÇÃO A — Desenvolvimento local: caminho para o arquivo de chave
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# OPÇÃO B — Railway/PaaS: cole o conteúdo COMPLETO do serviceAccountKey.json
# (string JSON em uma única linha — configure no painel do Railway em vez do .env)
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...}

# Porta do servidor de desenvolvimento local
PORT=3001

# Origens permitidas pelo CORS (separe múltiplas por vírgula)
# Em produção, coloque a URL da Vercel: https://sgfi.vercel.app
ALLOWED_ORIGINS=http://localhost:5173

JWT_SECRET=sgfi_secret_key_2026
